# Benchmarks

* Method, documents (B1–B4) and scenarios (S1–S9): `docs/PERFORMANCE.md`.
* Generated test files go in `bench/data/` (git-ignored).
* Every measurement is one row in `results.csv`, including Photoshop runs
  (`document = photoshop:<file>`).
