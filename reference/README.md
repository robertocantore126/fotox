# Reference code — read only, not compiled

`graphite-desktop/` is Graphite's desktop app (commit `ddafaaa`, MIT OR
Apache-2.0, licences included). It is here to be **ported** into
`crates/fx-app` and `tools/xtask` as described in `docs/GRAPHITE.md` §2.
`Cargo.toml` files are renamed `*.toml.txt` so Cargo ignores them.
Do not edit these files.
