# M1 — Open and navigate

Goal: open a 30 000 × 30 000 16-bit TIFF and navigate it smoothly, with RAM
under budget. One layer, no editing.

Exit criteria: S1, S2, S8 in `docs/PERFORMANCE.md` §4, measured and recorded.

Read first: `docs/ARCHITECTURE.md` §3–4, `docs/PERFORMANCE.md`, the doc
comments in `crates/fx-tiles/src/*.rs` and `crates/fx-render/src/*.rs`.

Spec tests already exist for several tasks, marked
`#[ignore = "M1-Txx"]`. Implementing a task includes removing its `ignore`
attributes and making those tests pass **without changing their assertions**.
If an assertion looks wrong, stop and ask.

---

## M1-T01 — `fotox-cli gen` and `info`

**Goal** Deterministic benchmark images B1 and B2 (`PERFORMANCE.md` §3).

**Files** `crates/fx-cli/src/{main.rs,gen.rs,info.rs}`, `crates/fx-cli/Cargo.toml` (+ `tiff`, `rayon`).

**Spec**
* `fotox-cli gen OUT.tif --width W --height H --bits 8|16 [--seed N]`
* RGB, no alpha, uncompressed strips of 256 rows, **BigTIFF** when the file
  exceeds 4 GiB (always for B1). If the `tiff` crate cannot stream-write
  BigTIFF strips, write a minimal BigTIFF writer by hand (uncompressed
  strips are ~150 lines) — say so in the report.
* Content, per pixel, from `(x, y, seed)` only (so bands can be generated in
  parallel and results are identical run to run): large smooth gradients +
  4-octave value noise (period 4096…512 px) + per-pixel grain (±1.5 % for
  16-bit). It must look photo-like and **not** compress well — this is
  deliberate.
* Generate band by band (256 rows) in parallel with rayon, write
  sequentially. Memory: ≤ 4 bands in flight.
* Print progress and final throughput (MB/s).
* `fotox-cli info FILE`: size, bits, channels, compression, strip/tile
  layout, BigTIFF yes/no, ICC profile present.

**Acceptance** B1 is generated in < 2 min on the reference machine; two runs
with the same seed produce byte-identical files (compare SHA-256); peak RSS
< 500 MB.

---

## M1-T02 — Streaming TIFF import

**Files** `crates/fx-io/src/{lib.rs,tiff.rs}`, `Cargo.toml` (+ `tiff`, `rayon`).

**Spec**
* `import_file` sniffs the header (`II*\0`, `MM\0*`, `II+\0`, `MM\0+`).
* Supported: 8/16-bit; RGB, RGBA (straight or associated alpha → convert to
  straight), Gray, Gray+alpha; strips or tiles; none/LZW/Deflate/PackBits;
  classic and BigTIFF. Everything else → `IoError::Unsupported("…")` with a
  clear reason.
* Band pipeline: decode 256 rows (strips) or one row of tiles, convert to
  RGBA of the document depth, cut into 256² tiles, `TiledImage::put_buffer`.
  Decoding and tile conversion of different bands run in parallel (rayon);
  inserts happen in order. Memory ≤ 4 bands.
* ICC profile embedded → `ColorProfile::Icc(bytes)`; none → `Srgb`.
  Resolution tags → `ppi` (default 72).
* Progress callback per band; returning `false` cancels (partial tiles are
  dropped with the `TiledImage`).
* Tests: small generated TIFFs covering each supported variant (write them
  with the `tiff` encoder in the test), pixel-exact comparison of a few
  tiles, including the right/bottom edge tiles (outside pixels must be
  transparent).

**Acceptance** Importing B1 reaches ≥ 80 % of the raw sequential read speed
of the file (measure raw speed by reading the file into `/dev/null`
equivalent, report both). Peak RSS stays under the hot+warm budget (needs
T03/T04 for the full B1; until then test with a 15 000² file).

---

## M1-T03 — Warm tier and `trim`

**Files** `crates/fx-tiles/src/store.rs`, `Cargo.toml` (+ `lz4_flex`).

**Spec** Implement `Residency::Warm`, the `Warm` branch of `get`, and
`trim` exactly as documented on `TileStore::trim`. LRU order: by
`last_use`. Candidate scan: iterate the registry shards, collect
`(last_use, weak)` of hot tiles, partial sort, process oldest first until
under budget. Skip tiles whose `Arc<TileBuffer>` has `strong_count > 1`.
Decompression promotes the tile back to hot. Accounting must stay exact
(tests compare `stats()`).

**Acceptance** The two `M1-T03` spec tests pass. Add a test that runs
`trim` concurrently with `get` from 8 threads for 2 s (no deadlock, all
reads bit-exact). Benchmark (`fotox-cli bench trim`, can be added in T10):
compress ≥ 1 GB/s aggregate on 8 threads for B1-like tiles.

---

## M1-T04 — Cold tier (scratch file) and the trim thread

**Files** `crates/fx-tiles/src/{store.rs,scratch.rs}`.

**Spec**
* Scratch file `fotox-<pid>.scratch` in `scratch_dir`, created on
  `TileStore::new`, deleted on drop. Opened with `FILE_FLAG_DELETE_ON_CLOSE`
  semantics on Windows if practical (so a crash does not leak 60 GB).
* Blocks = LZ4 data of one tile. Allocation: free list of extents (first
  fit, coalescing), else append. Freed when the tile entry drops.
* `trim` step 3: oldest warm tiles → cold (write outside any entry lock:
  write first, then swap the residency under the lock, re-checking the tile
  was not accessed meanwhile).
* Background trim thread: wakes when `insert`/`get` push `hot + warm`
  over budget (condvar/channel, never trims inline in `insert`), and every
  250 ms. Stops when the store drops.
* `scratch_limit` reached → `insert` of authoritative tiles still succeeds
  (they stay in RAM) but `stats()` reports it and the engine shows a
  warning; never crash, never silently lose data.

**Acceptance** The `M1-T04` spec test passes. Import B1 with default
budgets: RSS ≤ 10 GB throughout, all tiles readable afterwards (spot-check
1 000 random tiles against a re-decode).

---

## M1-T05 — Mip generation

**Files** `crates/fx-tiles/src/mip.rs`, `crates/fx-engine/src/mips.rs`.

**Spec**
* Implement `downsample_2x2` as documented; the four `M1-T05` tests pass.
* Resolving children: `Empty` slot or outside the grid → `ChildPixels::Empty`;
  if all four children are `Empty` the parent is `Empty`, if all four are
  the same `Solid` the parent is that `Solid` (no pixel work).
* `fx-engine::mips`: `ensure_mip(image, level, tx, ty)` computes a dirty
  tile recursively from dirty children, on the rayon pool, inserting results
  as `TileClass::Derived`. If `get` of a derived child returns `Evicted`,
  recompute it.
* After an import, schedule mips for the whole image in the background,
  top-level first is **not** possible (needs children) — so go level 1, 2, …
  but prioritise the tiles the current view needs.

**Acceptance** Tests above; `fotox-cli bench mips` on B1 (T10): full
pyramid in ≤ 10 s.

---

## M1-T06 — GPU tile atlas

**Files** `crates/fx-render/src/atlas.rs`.

**Spec** As in the module docs. API sketch (adjust if needed, document it):
```rust
pub struct TileAtlas { /* texture array pages, slot LRU */ }
impl TileAtlas {
    pub fn new(device: &wgpu::Device, budget_bytes: u64) -> Self;
    pub fn lookup(&mut self, key: AtlasKey, frame: u64) -> Option<AtlasSlot>;
    /// Queue an upload; returns the slot the data will occupy.
    pub fn upload(&mut self, key: AtlasKey, tile: &TileBuffer, frame: u64, queue: &wgpu_sync::Queue) -> AtlasSlot;
    pub fn begin_frame(&mut self, frame: u64);
}
```
* Conversion u8/u16 straight → f16 premultiplied on the CPU (rayon), into a
  reusable staging buffer; `write_texture` per tile.
* Page = one `texture_2d_array` with up to 256 layers of 256×256 (check
  `max_texture_array_layers`); add pages until the budget is reached.
* Slots used in the current frame are never evicted.

**Acceptance** Unit test on a real device (skip if no adapter): upload 1 000
random tiles, read back 20 and compare to the CPU conversion. Upload
throughput ≥ 3 000 tiles/s on the reference GPU.

---

## M1-T07 — Viewport renderer: one layer, progressive

**Files** `crates/fx-render/src/{compositor.rs,viewport_renderer.rs,*.wgsl}`,
`crates/fx-engine/src/render.rs`.

**Spec**
* Replace the M0 test pattern with: for the current document (one pixel
  layer), draw the visible tiles at `mip_level` from the atlas as textured
  quads into the viewport texture. Sampling: nearest when zoom ≥ 1
  (Photoshop shows hard pixels), linear below. Document outside the canvas:
  `#282828`; transparent pixels: Photoshop grey/white 8-px checkerboard in
  **screen** space.
* Progressive: a tile not in the atlas / not hot / mip dirty → use the
  nearest coarser level that is available (sub-rectangle of its parent);
  schedule the load (`TileStore::get` on a worker → upload budget next
  frame). Keep requesting frames until every visible tile is at the
  right level.
* Upload budget 48 tiles/frame; priority: current level, centre-out.
* The render thread never calls blocking `TileStore::get`.

**Acceptance** S2 on B1: ≥ 60 fps fit ↔ 3200 %, never a black/missing tile
(coarser fallback instead), full detail < 250 ms after stopping.

---

## M1-T08 — Open, close, tabs

**Files** `crates/fx-engine/src/{documents.rs,lib.rs}`, `crates/fx-app`,
`ui/js/*` (tabs, status bar), `Cargo.toml` (+ `rfd`).

**Spec**
* Action `dlg:open` (and `Ctrl+O`) in native mode → native file dialog
  (`rfd`, filters: TIFF, PNG, JPEG, All). The Fotox mock dialog stays for
  browser mode.
* Import runs as a job with `progress` messages; the document appears as
  soon as the import finishes (progressive display *during* import is a
  bonus, not required).
* `document_opened`, `active_document`, `view` messages; Fotox document tabs
  created from them (name, `@ zoom %`, `(RGB/16)`); tab click →
  `activate_document`; close button → `close_document`; `tab:close` action.
* Drag & drop of a file onto the window opens it (winit `DroppedFile`).
* Command line: `fotox.exe FILE…` opens files.

**Acceptance** Open B1, B2 and a small PNG in three tabs, switch between
them, close them; RAM returns near the empty-app baseline after closing
(tiles freed).

---

## M1-T09 — PNG and JPEG import

**Files** `crates/fx-io/src/{png.rs,jpeg.rs}`, `Cargo.toml` (+ `png`, `zune-jpeg`).

**Spec** Same band pipeline as TIFF. PNG: 8/16-bit, gray/RGB/alpha/palette,
interlaced PNGs may decode fully (they are never huge in practice — cap at
16 k × 16 k, else error). JPEG: baseline + progressive, 8-bit, ICC from APP2,
EXIF orientation applied. Tests like T02.

---

## M1-T10 — `fotox-cli bench`

**Files** `crates/fx-cli/src/bench.rs`, `bench/results.csv`.

**Scenarios** `import` (S1 engine part: file → tiles, MB/s), `trim`
(compress/decompress throughput), `mips` (full pyramid time), `scratch`
(spill + read-back throughput). Each appends a row to `bench/results.csv`
(format in `PERFORMANCE.md` §3) with the current `git rev-parse --short HEAD`.

---

## M1-T11 — Frame-time overlay and memory readout

* Toggle with `Ctrl+Alt+F` (action `debug:fps`): small overlay in the
  viewport corner with fps, frame time p50/p99 over the last 2 s, tiles
  uploaded this frame, pending tile loads.
* `status` message twice per second → Fotox status-bar memory readout
  (hot/warm/scratch/GPU).

---

## M1-T12 — M1 acceptance run

Run S1, S2, S8 on B1 and B2, record in `bench/results.csv`, attach a short
screen recording of S2. Rob runs the same on Photoshop.

**M1 checklist**
- [ ] All `M1-*` spec tests un-ignored and green
- [ ] S1, S2, S8 targets met (or gaps explained with measurements)
- [ ] No buffer proportional to document size anywhere (reviewed)
- [ ] App stays responsive during import (UI and pan/zoom of other tabs)
