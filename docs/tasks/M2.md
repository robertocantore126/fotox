# M2 — Layers and blend modes

Goal: a real layer stack — pixel layers, groups, masks, clipping, all blend
modes, six adjustment layers — composited lazily and fast, with live Layers
and History panels and undo.

Exit criteria: S4, S5, S6, S8 (`docs/PERFORMANCE.md`) on B3, recorded.

Read first: `docs/BLEND_MODES.md` (the spec), `docs/ARCHITECTURE.md` §4–5,
`crates/fx-core/src/command.rs`, `docs/reports/CLAUDE-core.md`. The
compositing core of this milestone is already implemented by Claude; the
remaining cards are commands, two adjustments, engine wiring and UI.

---

## M2-T01 — Layer commands

Implement every command marked `M2` in `fx-core::command::Command`, with
validation first and mutation after (all or nothing). Rules:

* `AddLayer`: above the active layer, inside its group if the active layer
  is in a group; becomes the only selected layer. Default names like
  Photoshop: "Layer 1", "Group 1", "Curves 1"… (counter per kind, per document).
* `DeleteLayers`: deleting a group deletes its children; selection moves to
  the layer below (or above if none).
* `DuplicateLayers`: " copy" suffix; tile handles are shared (no pixel copy).
* `MoveLayer`: reject moving a group into itself or its descendants.
* `OffsetLayer`: pixel layers only (+ mask if linked); rejects `locked_position`.
* `AddMask RevealAll/HideAll` = `Empty`/`Solid` mask tiles (no memory).
* `DeleteMask {apply: true}`: multiply alpha by the mask — a pixel operation
  over the mask's non-uniform tiles only, on rayon.
* Every command: an effect (`CommandEffect`) listing exactly what changed.

Tests: one per command (success + each error), plus undo/redo round trips
through `History`. Plus `fx-engine`: `Document` → `Vec<LayerInfo>` conversion
(top → bottom, depth).

## M2-T02 — Blend modes ✅ done by Claude

`fx-render::blend` (f64 reference) + `gpu/composite.wgsl`. All 27 modes
match on GPU vs CPU (`gpu::tests::every_blend_mode_matches_the_reference`).

## M2-T03 — Stack compositor ✅ done by Claude

`fx-render::program` builds per-tile programs (groups isolated/pass-through,
clipping groups, masks, offsets, solid fills, adjustments, empty-tile
skipping, content-identity cache keys); the GPU compositor runs them.
Tested against the CPU reference on a complex stack.

## M2-T04 — Adjustment layers (remaining part)

Done: the LUT mechanism and Invert, Levels, Curves, Exposure
(`fx-render::adjust`). Yours:
1. **Brightness/Contrast** — bake as a LUT in `adjust::bake` (replace the
   `todo!`). Photoshop's modern (non-legacy) mode is a curve; `legacy: true`
   is linear `(x − 0.5)·c + 0.5 + b`. Write the CPU function + tests; the GPU
   path needs nothing more (it uses the LUT).
2. **Hue/Saturation** (master) — per pixel, not a LUT:
   * CPU: in `reference.rs`, replace the `todo!` for `AdjustKind::HueSaturation`.
   * GPU: add kind `K_ADJUST_HUESAT = 2` to `composite.wgsl` and
     `compositor.rs` (`encode`: params in `GpuOp.params` =
     `[hue, saturation, lightness, colorize]`), and remove the rejection in
     `check_supported`.
   * Test: extend `gpu/tests.rs` with a stack using it (GPU vs CPU).
3. UI: the existing Fotox adjustment dialogs send `SetAdjustment` on every
   change (live) when editing an adjustment *layer*.

VERIFY formulas against Photoshop in M7 (note them in the report).

## M2-T05 — Stack split cache (engine side)

The prefix cache is implemented in `GpuCompositor` (`set_hot_layer`): while
a layer is hot, everything below its root-level segment is composited once
and reused. Yours: in the engine, call `set_hot_layer(Some(id))` when a
layer receives ≥ 2 edits within 1 s (slider drags, painting), and
`set_hot_layer(None)` after 2 s idle. Measure S5 with and without; report
both. (A cache for the layers *above* the hot one is not implemented; only
add it if S5 misses its target — ask first.)

---

## M2-T06 — Layers and History panels, live

Fotox Layers panel driven by `layers` messages: thumbnails placeholder,
eye, name (double-click rename), blend drop-down, opacity/fill fields and
scrubby sliders, lock toggles, mask indicator, group expand/collapse,
multi-select (Ctrl/Shift), drag to reorder/into groups, footer buttons
(new layer, group, mask, adjustment menu, delete). Every change →
`command`. History panel from `history`; Ctrl+Z / Ctrl+Shift+Z / Ctrl+Alt+Z
like Photoshop. Must stay fluid with 1 000 layers (virtualised list).

## M2-T07 — Layer thumbnails

Engine renders each requested layer alone at the deepest mip level that
gives ≥ `size` px, scales to fit, sends a binary `thumbnail`. Throttle:
re-render a layer's thumbnail at most once per 500 ms while it changes.

## M2-T08 — B3 and M2 acceptance

`fotox-cli gen --preset b3` can only write TIFF until M3, so add an app
action `debug:load-b3` that builds B3 in memory from B1 (`PERFORMANCE.md` §3).
Measure S4, S5, S6, S8; record; Rob compares with Photoshop on an equivalent
PSD.

**M2 checklist**
- [ ] All blend modes + groups + clipping + masks match the CPU reference
- [ ] S4, S5, S6, S8 met on B3 (or gaps explained with measurements)
- [ ] Undo/redo of every command, History panel correct
- [ ] Layers panel fluid with 1 000 layers
