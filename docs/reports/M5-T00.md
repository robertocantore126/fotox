# M5-T00 — Decisions

Agent: Buffy (Codebuff)  ·  Branch: `m5`  ·  Status: ready for review

## Plan (written before coding)

- Record the ten accepted answers of the M5 card as D-040…D-049 in
  `docs/DECISIONS.md` (the next free number after M4's D-039).
- Mark the M5 card approved and note that T00 is done.
- Mark S13–S15 accepted in `docs/PERFORMANCE.md` (they were "proposed").
- Update the roadmap's current-milestone line.
- No code: nothing that depends on these answers is written before they are
  recorded (HOWTO.md R0.1).

## What was done

- `docs/DECISIONS.md`: D-040 selection pixels use the document's grey format
  with an integer offset; D-041 brush engine on the CPU (rayon + stroke
  thread), f32; D-042 Photoshop's stroke model (flow builds coverage, opacity
  as ceiling); D-043 the eraser always erases to transparency; D-044 clone/heal
  source = the document at stroke start; D-045 healing shows the clone during
  the drag and solves Poisson at pen-up, spot healing by proximity match;
  D-046 pen input through Windows Ink (`WM_POINTER`); D-047 the pure blend
  maths move to `fx-core::blend`; D-048 internal clipboard with a CF_DIBV5 copy
  below 8 192²; D-049 "Lock transparent pixels" added in M5.
- `docs/tasks/M5.md`: header marked approved, T00 section marked done.
- `docs/PERFORMANCE.md`: S13–S15 accepted; the "proposed" note now covers only
  the M6 scenarios.
- `docs/ROADMAP.md`: current milestone M5.

## Files changed

| File | Change |
| --- | --- |
| `docs/DECISIONS.md` | +10 rows, D-040…D-049 |
| `docs/tasks/M5.md` | approved, T00 done |
| `docs/PERFORMANCE.md` | S13–S15 accepted |
| `docs/ROADMAP.md` | current milestone M5 |

## Verification

```text
Docs only — no code. cargo fmt/clippy/test unaffected and run at the end of
each following card.
```

## Deviations from the task card

- none

## Blocked

- none

## Open problems / suggestions

- D-047 (moving the blend maths to `fx-core`) is a prerequisite of the brush
  work (T06), not of this slice; it is recorded now but the move happens with
  the first brush card that needs it.
