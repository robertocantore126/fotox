# M0-T07 — Polish and M0 acceptance

Agent: Claude  ·  Branch: `task/M0-T07-polish`  ·  Status: code done; **M0 checklist run is Rob's**

## What was done

1. **App icon and version info.** `crates/fx-app/build.rs` (port of `reference/graphite-desktop/platform-win/build.rs`, `winres` as a Windows build-dependency — already in `[workspace.dependencies]`). `ui/assets/` has no raster icon (only the SVG sprite), so `crates/fx-app/assets/fotox.ico` is generated from the UI's logo mark (blue rounded square, white "F") by `assets/make_icon.py` (pure Python, 16–256 px). A missing `rc.exe` fails the build with a clear message.
2. **README "Build and run"** updated: CMake/Ninja on `PATH`, `rc.exe`, `xtask bundle`/`--release`, log filters (`fotox=`), DevTools port, sharing CEF between worktrees. Not re-verified on a clean clone (a full CEF build takes several minutes; left for the checklist run).
3. **AGENTS.md**: `-p xtask` in the fmt check (raised in M0-T02 and M0-T03), `RUST_LOG=fotox=…`, CMake on `PATH`.

## Verification

```text
cargo build -p fx-app: ok
target/debug/fotox.exe: FileDescription "Fotox", ProductName "Fotox", FileVersion 0.0.0.0,
  OriginalFilename fotox.exe; icon extracted 32×32, white "F" at the centre
```

## M0 checklist (for Rob)

- [x] `cargo test` green, `cargo clippy --workspace` clean (our crates) — Claude, 2026-09-25
- [ ] `cargo xtask run` works from a clean clone (after installing prerequisites)
- [ ] Full Fotox UI usable inside the app, identical to browser mode
- [ ] Viewport hole correct at 100/125/150 % scaling and all screen modes — 100 % and all three screen modes verified pixel-exact in M0-T04; 125/150 % open
- [ ] Pan/zoom ≥ 60 fps, cursor-anchored zoom, rulers in sync
- [ ] Popups block viewport input; strokes captured until release
- [ ] UI still works in a plain browser with the mock engine
- [ ] Closing the app leaves no processes — verified for M0-T03; re-check with the engine threads
